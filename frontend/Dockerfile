# Используем официальный образ Nginx как базовый.
# 'alpine' - легковесная версия Linux.
FROM nginx:stable-alpine

# Копируем все файлы из текущей директории (нашего фронтенда)
# в стандартную папку, из которой Nginx раздает статику.
COPY . /usr/share/nginx/html

# Сообщаем Docker, что контейнер будет слушать порт 80.
EXPOSE 80